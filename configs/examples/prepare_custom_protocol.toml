[prepare_data]
data_root = "/path/to/omnichainbench"
prepared_root = "/path/to/omnichainbench_prepared"
media_formats = ["frames", "sampled_video"]
generate_oracle_visual_media = true
workers = 8
protocols = ["omnichain_eval.example_protocols:ExampleEightFrameUniformProtocol"]
